# Docker compose deployment settings
CORS_ORIGINS=http://localhost:8080,http://127.0.0.1:8080
ML_CONFIDENCE_THRESHOLD=0.2

# Optional path to a trained model inside backend container or mounted volume
# ML_MODEL_PATH=/app/models/move_model.joblib

# Optional. Leave empty to use same-origin /api via Nginx proxy.
VITE_API_BASE_URL=
